// PsyFi WGSL module stub — replace with production pass in roadmap G1+.
// Contract: consume snapshot uniforms/buffers only; no symbolic inference.
@fragment
fn main_fs() -> @location(0) vec4<f32> {
  return vec4<f32>(0.0, 0.0, 0.0, 1.0);
}
