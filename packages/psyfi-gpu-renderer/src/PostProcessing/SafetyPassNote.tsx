/** Documents that post.safety is mandatory; real attenuator runs in PresentPipeline. */
export function SafetyPassNote(_props: {
  maxLuminanceDelta: number
  maxFlashHz: number
  enabledPasses: string[]
  neutral: boolean
}) {
  return null
}
