# PsyFi - MIDI-Enabled Deployment Requirements
# Use this for local/VPS deployments with full MIDI support

# Core dependencies (from requirements.txt)
numpy>=1.24.0
scipy>=1.10.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
jinja2>=3.1.0
python-multipart>=0.0.6

# MIDI integration (requires system dependencies)
# Linux: apt-get install libasound2-dev libjack-dev
# macOS: brew install portmidi
mido>=1.3.0
python-rtmidi>=1.5.0
